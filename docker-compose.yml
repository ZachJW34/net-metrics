services:
  net-metrics:
    container_name: net-metrics
    build: .
    environment:
      - NODE_ENV=production
      - PORT=${PORT}
      - SERVER_ID=${SERVER_ID}
    volumes:
      - ./db:/app/db
    network_mode: host
    restart: always
